<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/contact/edit/{{contact.id}}" method="POST">
                        <div class="form-group">
                            <input type="email" value="{{contact.email}}" class="form-control" name="email-adress" placeholder="Title"> 
                        </div>
                        <div class="form-group">
                            <input type="title" value="{{contact.title}} class="form-control" name="title" placeholder="Title"> 
                        </div>
                        <div class="form-group">
                            <textarea type="text" value="{{contact.description}}" name="description" class="form-control" placeholder="Description"></textarea>
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Save
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>